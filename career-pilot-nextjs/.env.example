# ============================================
# CareerPilot — Environment Variables
# Copy this to .env.local and fill in values
# ============================================

# MongoDB Atlas (free tier at mongodb.com/atlas)
# Format: mongodb+srv://username:password@cluster.mongodb.net/careerpilot?retryWrites=true&w=majority
DATABASE_URL="mongodb+srv://youruser:yourpassword@cluster0.xxxxx.mongodb.net/careerpilot?retryWrites=true&w=majority"

# Auth.js Secret (generate with: openssl rand -base64 32)
AUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# ---- OAuth Providers ----

# Google (console.cloud.google.com)
# 1. Create project → Enable "Google+ API"
# 2. OAuth 2.0 → Create credentials
# 3. Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# GitHub (github.com/settings/applications/new)
# Homepage URL: http://localhost:3000
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# LinkedIn (developer.linkedin.com/apps)
# 1. Create app → Products → "Sign In with LinkedIn using OpenID Connect"
# 2. Auth → Authorized redirect URLs: http://localhost:3000/api/auth/callback/linkedin
LINKEDIN_CLIENT_ID=""
LINKEDIN_CLIENT_SECRET=""

# Microsoft / Azure AD (portal.azure.com)
# 1. App Registrations → New registration
# 2. Supported account types: "Any Azure AD + personal Microsoft accounts"
# 3. Redirect URI: http://localhost:3000/api/auth/callback/microsoft-entra-id
# 4. Tenant ID: "common" works for any Microsoft account
AZURE_AD_CLIENT_ID=""
AZURE_AD_CLIENT_SECRET=""
AZURE_AD_TENANT_ID="common"

# ---- Email / OTP ----
# Use Gmail, Resend, or any SMTP provider
# For Gmail: enable "App Passwords" in your Google Account security settings
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your@gmail.com"
SMTP_PASSWORD="your-app-password"

# ---- Optional ----
# OpenAI API Key (for real resume analysis)
# OPENAI_API_KEY=""
