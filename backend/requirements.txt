# Web framework
fastapi
uvicorn[standard]
python-multipart

# xAI / OpenAI-compatible SDK
openai
httpx

# PDF
pypdf

# LangChain — text splitter only (no vectorstore, no embeddings needed)
langchain-core
langchain-text-splitters

# Env
python-dotenv